<form class="ui raised segment form" action="/group/{{group.id}}/addmember" method="POST">
    <h3 class="ui header">Add Member</h3>

    <div class="field">
        <label>First Name</label>
        <input placeholder="First Name" type="text" name="firstName" value="{{user.firstName}}" required>
    </div>
    <div class="field">
        <label>Last Name</label>
        <input placeholder="Last Name" type="text" name="lastName" value="{{user.lastName}}" required>
    </div>
    <div class="field">
        <label>Email</label>
        <input placeholder="Email" type="text" name="email" value="{{user.email}}" required>
    </div>


    <button class="ui blue submit button">Add Member</button>
</form>


<section class="ui inverted green center aligned segment">
    <section class="ui large inverted yellow segment">
        <div class="ui accordion">
            <div class="title">
                <i class="dropdown icon"></i>
                Add Member
            </div>
            <div class="content">
                <form class="ui raised segment form" action="/group/{{group.id}}/addmember" method="POST">
                    <h3 class="ui header">Add Member</h3>

                    <div class="field">
                        <label>First Name</label>
                        <input placeholder="First Name" type="text" name="firstName" value="{{user.firstName}}" required>
                    </div>
                    <div class="field">
                        <label>Last Name</label>
                        <input placeholder="Last Name" type="text" name="lastName" value="{{user.lastName}}" required>
                    </div>
                    <div class="field">
                        <label>Email</label>
                        <input placeholder="Email" type="text" name="email" value="{{user.email}}" required>
                    </div>


                    <button class="ui blue submit button">Add Member</button>
                </form>
            </div>
        </div>
    </section>
</section>

<script>
    // for dropdown feature
    $('.ui .accordion')
            .accordion({
                selector: {
                    trigger: '.title .icon'
                }
            })
    ;
</script>