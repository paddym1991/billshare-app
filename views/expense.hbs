{{> expensepanel id="expense"}}

<section class="ui center aligned middle aligned segment">
    <h2 class="ui header">
        {{expense.title}}: Payments
    </h2>
    {{> listpayments}}
    {{> addpayment}}
</section>